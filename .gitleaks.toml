# Gitleaks configuration for Harmon Dotfiles
# https://github.com/gitleaks/gitleaks

[extend]
useDefault = true

[allowlist]
paths = [
    '''\.git/''',
    '''\.task/''',
    '''\.worktrees/''',
    # BMad installer content-hash manifest.
    '''_bmad/_config/files-manifest\.csv''',
]
