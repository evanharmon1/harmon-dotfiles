# chezmoi:managed — edit via 'chezmoi edit' or in ~/git/harmon-dotfiles; direct edits are overwritten by the next 'chezmoi apply' unless re-added
# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# Logs and databases #
######################
*.log
*.sql
*.sqlite

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

#             .env
#------------------------------------------------------------------------------
# Ignore all .env files
.env
# Ignore all .env files in any subdirectory
**/.env
# Ignore all .env.* files (e.g., .env.local, .env.production)
.env.*
# Ignore all .env.* files in any subdirectory
**/.env.*
# Ignore any directory named .env
.env/


#             Evan's
#------------------------------------------------------------------------------

**/.claude/settings.local.json

#             VS Code
#------------------------------------------------------------------------------
# Extensions auto-write machine-specific values into .vscode/settings.json
# (absolute paths, per-install state), so it churns endlessly. Keep it local.
# A repo can still commit a curated settings.json if it holds shared config.
**/.vscode/settings.json

**/.claude/.cc-writes/
